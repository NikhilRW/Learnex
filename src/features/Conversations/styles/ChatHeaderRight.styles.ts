import {StyleSheet} from 'react-native-unistyles';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  notificationsButton: {
    marginRight: 15,
  },
  deleteButton: {
    marginRight: 10,
  },
});

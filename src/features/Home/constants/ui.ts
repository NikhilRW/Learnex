/** Minimum percentage of item visible before it counts as "viewable" */
export const VIEWABLE_ITEM_THRESHOLD = 90;

/** Estimated height of a single post item in the feed list */
export const ESTIMATED_POST_ITEM_SIZE = 500;

/** Maximum number of trending tags shown in the header */
export const TRENDING_TAGS_LIMIT = 10;

/** Number of recent posts used for trending tag calculation */
export const TRENDING_POSTS_LIMIT = 20;

import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
  title: {
    color: 'black',
    fontSize: 20,
  },
  avatar: {
    borderRadius: 50,
    width: 60,
    height: 60,
  },
});
